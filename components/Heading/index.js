// textAlign, content, level are info that comes from Wordpress via API and I want to define them!
const Heading = ({ textAlign, content, level }) => {
  return <div>This is Heading</div>
}

export default Heading
